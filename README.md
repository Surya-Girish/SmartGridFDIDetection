# SmartGridFDIDetection
Final sem project for fourth year. Smartgrid False Data Injection Detection use case. Base paper uses XTM(Transformer+LSTM) to detect and localise FDIs. We propose to use a GNN+Transformer+CFE to detect, localise and pinpoint the origin of the FDI.
